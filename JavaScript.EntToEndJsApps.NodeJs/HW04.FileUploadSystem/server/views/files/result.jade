extends ../shared/layout

block main-content
    h1 Files
    hr
    table.table.table-bordered
        tr
            th File name
            th Private
        each file in files
            tr
                td
                    a(href='/files/download/#{file.url}') #{file.fileName}
                td #{file.isPrivate ? 'Yes' : 'No'}